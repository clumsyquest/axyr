# Telemetry over SEGGER RTT (non-intrusive: writes a RAM ring buffer, no halt).
# Route the console to RTT so printk() goes out the RTT up-channel.
CONFIG_USE_SEGGER_RTT=y
CONFIG_RTT_CONSOLE=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=n
